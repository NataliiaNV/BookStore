language: python
python:
  - 3.8.10
install:
  - pip install -r requirements.txt

scripts:
  - python -m unittest bookstore_app.tests.genre_tests
notifications:
  email:
    reciplients:
      - vasylivna.nv@gmail.com
    on_success: never
    on_failure: always